name: CI Orchestrator

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:
  workflow_call:
permissions:
  contents: write
  pull-requests: write
  security-events: write
  actions: read

jobs:
  labeler:
    name: Label PR
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/labeler@v6
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          configuration-path: .github/labeler.yml

  lint-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14.5'

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements_test.txt ]; then python -m pip install -r requirements_test.txt; fi
          python -m pip install ruff black pytest pytest-asyncio pytest-cov homeassistant oci pytest-homeassistant-custom-component

      - name: Lint Python
        run: |
          black --check .
          ruff check .

      - name: Run Tests
        run: |
          python -m pytest



  hassfest:
    name: Validate with Hassfest
    runs-on: ubuntu-latest
    needs: lint-test
    steps:
      - uses: actions/checkout@v6
      - uses: home-assistant/actions/hassfest@master

  hacs-validation:
    name: Validate HACS
    runs-on: ubuntu-latest
    needs: lint-test
    steps:
      - uses: actions/checkout@v6
      - name: HACS validation
        uses: hacs/action@main
        with:
          category: integration

  codeql:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [ 'python' ]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}

      - name: Autobuild
        uses: github/codeql-action/autobuild@v4

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4

  auto-merge:
    name: Auto Merge Renovate
    runs-on: ubuntu-latest
    needs: [lint-test, hassfest, hacs-validation, codeql]
    if: github.event_name == 'pull_request' && github.actor == 'renovate[bot]'
    steps:
      - uses: actions/checkout@v6
      - name: Enable Auto Merge
        run: gh pr merge --squash "$PR_URL"
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
